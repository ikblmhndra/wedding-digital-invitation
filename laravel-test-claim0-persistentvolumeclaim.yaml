apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: laravel-test-claim0
  name: laravel-test-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
