apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: laravel-test
  name: laravel-test
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
    - name: "5173"
      port: 5173
      targetPort: 5173
  selector:
    io.kompose.service: laravel-test
